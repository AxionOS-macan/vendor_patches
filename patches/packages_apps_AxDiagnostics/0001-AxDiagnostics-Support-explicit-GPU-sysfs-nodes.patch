From 98bb261b5dc5e665a1f33178240eaabd36ae904f Mon Sep 17 00:00:00 2001
From: blazerpaul15 <blazerpaul15@gmail.com>
Date: Mon, 20 Jul 2026 10:41:11 +0900
Subject: [PATCH 1/2] AxDiagnostics: Support explicit GPU sysfs nodes

Allow GPU configs to provide explicit min/max/governor nodes instead of
assuming devfreq-style min_freq, max_freq and governor files.

This fixes canoe KGSL reporting, where min/max clocks are exposed through
min_clock_mhz and max_clock_mhz, and avoids displaying Hz values above
1GHz as 1MHz.

Fix KGSL GPU busy percentage parsing, KGSL reports gpu_busy_percentage with a space before the percent sign
(e.g. "30 %"). Trim after removing the percent sign so the value parses
correctly instead of falling back to 0.

Change-Id: Iaa2f07e4ecb9ed3a75080b41d646328596ca9baa
---
 .../com/axion/diagnostics/data/GpuInfo.kt     | 50 +++++++++++--------
 .../axion/diagnostics/data/KernelConfig.kt    | 18 +++++--
 2 files changed, 42 insertions(+), 26 deletions(-)

diff --git a/app/src/main/java/com/axion/diagnostics/data/GpuInfo.kt b/app/src/main/java/com/axion/diagnostics/data/GpuInfo.kt
index 622c537..a712a94 100644
--- a/app/src/main/java/com/axion/diagnostics/data/GpuInfo.kt
+++ b/app/src/main/java/com/axion/diagnostics/data/GpuInfo.kt
@@ -43,31 +43,14 @@ object GpuCollector {
                     val busyFile = File(config.usageNode)
                     if (busyFile.exists()) {
                         runCatching {
-                            busyFile.readText().trim().replace("%", "").toInt()
+                            busyFile.readText().replace("%", "").trim().toInt()
                         }.getOrDefault(0).coerceIn(0, 100)
                     } else 0
                 } else 0
 
-                val governor = if (config.node != null) {
-                    val govFile = File(config.node, "governor")
-                    if (govFile.exists()) runCatching { govFile.readText().trim() }.getOrDefault("unknown") else "unknown"
-                } else "unknown"
-
-                val maxFreq = if (config.node != null) {
-                    val maxFile = File(config.node, "max_freq")
-                    if (maxFile.exists()) {
-                        val rawMax = runCatching { maxFile.readText().trim().toLong() }.getOrDefault(0L)
-                        KernelConfig.scaleFreqToMhz(rawMax)
-                    } else 0
-                } else 0
-
-                val minFreq = if (config.node != null) {
-                    val minFile = File(config.node, "min_freq")
-                    if (minFile.exists()) {
-                        val rawMin = runCatching { minFile.readText().trim().toLong() }.getOrDefault(0L)
-                        KernelConfig.scaleFreqToMhz(rawMin)
-                    } else 0
-                } else 0
+                val governor = readGovernor(config)
+                val maxFreq = readGpuFrequency(config.maxNode, config.node, "max_freq")
+                val minFreq = readGpuFrequency(config.minNode, config.node, "min_freq")
 
                 if (curFreq > 0) {
                     return GpuSnapshot(
@@ -85,4 +68,29 @@ object GpuCollector {
 
         return GpuSnapshot(0, 0, 0, 0, "unavailable", emptyList(), false)
     }
+
+    private fun readGpuFrequency(path: String?, node: String?, fallbackName: String): Int {
+        val file = path?.let { File(it) } ?: node?.let { File(it, fallbackName) } ?: return 0
+        if (!file.exists()) return 0
+        val raw = runCatching { file.readText().trim().toLong() }.getOrDefault(0L)
+        return if (file.name.endsWith("_clock_mhz")) {
+            raw.toInt()
+        } else {
+            KernelConfig.scaleFreqToMhz(raw)
+        }
+    }
+
+    private fun readGovernor(config: GpuConfig): String {
+        val file = config.governorNode?.let { File(it) }
+            ?: config.node?.let { File(it, "governor") }
+            ?: return "unknown"
+        if (!file.exists()) return "unknown"
+        val value = runCatching { file.readText().trim() }.getOrDefault("unknown")
+        return when {
+            file.name == "pwrscale" && value == "1" -> "pwrscale"
+            file.name == "pwrscale" && value == "0" -> "fixed"
+            value.isBlank() -> "unknown"
+            else -> value
+        }
+    }
 }
diff --git a/app/src/main/java/com/axion/diagnostics/data/KernelConfig.kt b/app/src/main/java/com/axion/diagnostics/data/KernelConfig.kt
index 08da602..486bd4d 100644
--- a/app/src/main/java/com/axion/diagnostics/data/KernelConfig.kt
+++ b/app/src/main/java/com/axion/diagnostics/data/KernelConfig.kt
@@ -35,6 +35,9 @@ data class GpuConfig(
     val node: String?,
     val currentNode: String?,
     val usageNode: String?,
+    val minNode: String?,
+    val maxNode: String?,
+    val governorNode: String?,
     val frequencyMultiplier: Long,
     val values: List<Int>,
 )
@@ -52,9 +55,8 @@ object KernelConfig {
 
     fun scaleFreqToMhz(raw: Long): Int {
         return when {
-            raw > 1_000_000_000L -> (raw / 1_000_000_000L).toInt()
-            raw > 1_000_000L -> (raw / 1_000_000L).toInt()
-            raw > 1_000L -> (raw / 1_000L).toInt()
+            raw >= 1_000_000L -> (raw / 1_000_000L).toInt()
+            raw >= 1_000L -> (raw / 1_000L).toInt()
             else -> raw.toInt()
         }
     }
@@ -102,11 +104,14 @@ object KernelConfig {
 
     private fun parseGpuConfig(): GpuConfig {
         val file = File(CONFIG_PATH)
-        if (!file.exists()) return GpuConfig(null, null, null, 1L, emptyList())
+        if (!file.exists()) return GpuConfig(null, null, null, null, null, null, 1L, emptyList())
 
         var node: String? = null
         var currentNode: String? = null
         var usageNode: String? = null
+        var minNode: String? = null
+        var maxNode: String? = null
+        var governorNode: String? = null
         var multiplier = 1L
         var values = emptyList<Int>()
 
@@ -120,6 +125,9 @@ object KernelConfig {
                         node = parser.getAttributeValue(null, "node")
                         currentNode = parser.getAttributeValue(null, "currentNode")
                         usageNode = parser.getAttributeValue(null, "usageNode")
+                        minNode = parser.getAttributeValue(null, "minNode")
+                        maxNode = parser.getAttributeValue(null, "maxNode")
+                        governorNode = parser.getAttributeValue(null, "governorNode")
                         val multStr = parser.getAttributeValue(null, "frequencyMultiplier")
                         multiplier = multStr?.toLongOrNull() ?: 1L
 
@@ -133,6 +141,6 @@ object KernelConfig {
                 }
             }
         }
-        return GpuConfig(node, currentNode, usageNode, multiplier, values)
+        return GpuConfig(node, currentNode, usageNode, minNode, maxNode, governorNode, multiplier, values)
     }
 }
